---
name: interview-qbank-builder
description: 多人复用的求职面试题库与口语化自我介绍构建器。在进行题库与自我介绍构建前，必须先交互式问清候选人真实背景、目标岗位 JD 和知识库链接；在生成题库阶段必须通过 agent-reach 调用小红书搜索岗位面试经验与高频题目，若登录态失效则先停下等待用户登录后再继续，严禁虚构经历。
---

# Interview Qbank Builder

为不同候选人与目标岗位构建事实受控、口语化、可直接练习的面试材料。

## 核心原则

1. 零虚构与事实受控：严格区分“独立主导”“核心参与”“协助协同”和“仅了解”，不能把 JD 要求或小红书经验写成候选人事实。
2. 前置三要素门禁：没有候选人背景、目标岗位 JD、知识库链接或明确的“无知识库”确认时，不开始分析和生成。
3. 小红书经验是外部参考，不是候选人事实：小红书内容只用于识别岗位面试风格、常见追问和高频主题，必须标注为公开经验或样本信号，并保留来源链接或标题（如工具返回）。
4. 登录问题必须停机等待：小红书认证失败、登录过期、缺少 a1 Cookie、Session expired、No a1 cookie found 或搜索无法在认证前完成时，停止后续题库生成，明确告诉用户先完成登录；用户确认完成后再从认证检查继续。
5. 回答采用三段论：结论先行 -> 真实案例或量化证据 -> 落到目标岗位。语言应谦逊、自然、适合现场口述。

## 工作流

### 第一步：采集并确认三要素

在生成任何分析或题库前，确认以下信息：

1. 候选人真实背景：学历、专业、实习/工作经历、项目、职责边界、技术栈和熟练程度。
2. 目标岗位 JD：公司、岗位名称、职责、任职要求、技术关键词和面试轮次（如已知）。
3. 知识库或事实材料：Obsidian、语雀、Notion、飞书、本地文件、工作日志或项目链接；若没有，必须让用户明确回复“无”或“以当前提供材料为准”。

信息缺失时，一次性列出缺口并等待补充，不要依据岗位名称或常见简历模板猜测候选人经历。

### 第二步：建立事实边界和 JD 映射

- 提取已证实事实、待确认事实、不可声称事实三类清单。
- 将 JD 映射为高匹配区、可迁移区和能力空白区。
- 对每个核心项目记录候选人角色：主导、参与、协助或了解。
- 将所有不确定数字、技术深度和项目归属标记为待确认，不自动补齐。

### 第三步：调用 agent-reach 搜索小红书面经（生成题库前的强制步骤）

在开始生成 20~30 道题库前，必须使用已安装的 agent-reach Skill 和 xhs CLI 搜索相关公开经验。详细命令与认证处理见 references/xhs-search.md。

#### 3.1 认证预检

按顺序执行轻量认证检查：

xhs status
xhs whoami

以下任一情况出现时，立即停止，不得先生成题库，也不得静默改用其他搜索来源替代：

- 命令不可用或 agent-reach/xhs 渠道未配置；
- 返回未认证、登录过期或 Session expired；
- 出现 No a1 cookie found、Cookie 缺失或浏览器登录态提取失败；
- 搜索接口因认证失败无法可靠返回结果。

停下时向用户说明：小红书登录态未通过，已暂停题库生成，请用户完成扫码或浏览器登录后回复“已登录”；不要索取、保存、展示 Cookie、Token 或完整认证信息。用户回复后重新运行 xhs status 与 xhs whoami，通过后才继续。

#### 3.2 检索范围

根据公司、岗位、行业和面试轮次生成 2~4 个查询，至少覆盖：

- 公司名 + 岗位名 + 面试
- 岗位名 + 面试题
- 公司名 + 面经
- 行业或技术方向 + 岗位名 + 高频题

使用 xhs search 查询词 获取结果；需要核实时，使用搜索结果中的 URL 或带上下文的 ID 调用 xhs read，不要使用裸 note_id。必要时读取少量评论，但保持低频，每次请求间隔约 2~3 秒，避免批量抓取和触发验证码。

#### 3.3 证据整理

只提取与目标岗位直接相关的公开信号：高频题目和追问主题、面试轮次、题型、技术深度、业务场景，以及内容发布时间、笔记标题、作者或 URL（工具能提供时）。

不要复制整篇笔记，不要把单条笔记当成普遍事实。重复出现的主题标记为“多条样本出现”，只出现一次的内容标记为“单样本线索”，无法核实的内容标记为“待验证”。外部内容不得覆盖候选人自己的简历、工作日志或知识库事实。

### 第四步：生成一分钟自我介绍

- 控制在约 220~280 字，正常语速约 60~75 秒。
- 只选择与目标岗位最匹配且已证实的 1~2 个经历。
- 使用沉稳、谦逊、自然的口语，不加入没有证据的成果或职责。

### 第五步：生成 20~30 道面试题库

题库应同时吸收三类证据：候选人事实、目标岗位 JD、小红书公开面经信号。小红书仅影响“问什么”和“追问方向”，不改变“候选人做过什么”。建议覆盖项目深挖与技术实战题 8~12 题、岗位职责与工作方法题 4~6 题、跨部门协同与异常处理题 4~6 题、求职动机与职业规划题 4~6 题。

每题包含：题目、考察重点、参考回答（结论 -> 真实案例或量化 -> 落到岗位）、可能追问、避坑提示。可能追问优先参考小红书中重复出现的主题，同时避免照搬单条经验。

题库正文前增加“小红书检索摘要”，列出使用过的查询、代表性来源、重复高频主题、单样本线索和认证或覆盖限制。若检索未完成，不能伪造摘要；应保留暂停状态或明确说明缺失。

### 第六步：反问清单与临场速记卡

输出 3~5 个高价值反问、3 个核心经历标签、2 条表达红线和 1 句岗位匹配总结。

## 质量核验清单

- [ ] 三要素是否完整确认？
- [ ] 是否在题库生成前完成 agent-reach 小红书认证检查和岗位检索？
- [ ] 登录态有问题时是否已停下等待用户，而不是继续生成？
- [ ] 小红书内容是否标注为外部公开经验，并与候选人事实分开？
- [ ] 所有候选人项目、数字、技术选型和职责是否有来源？
- [ ] 每题是否包含考察重点、三段式回答、追问和避坑提示？
- [ ] 自我介绍是否适合一分钟口述且避免过度承诺？
