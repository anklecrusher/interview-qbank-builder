---
name: interview-qbank-builder
description: 多人复用的求职面试题库与口语化自我介绍构建器。构建前必须先确认候选人背景、目标岗位 JD、知识库链接、公司名称与岗位名称；输出到按公司划分的题库目录，并采用可适配不同岗位与经历的稳健工科口语风。小红书面经检索是可选模块，只有用户明确要求时才通过 agent-reach 开启。
---

# Interview Qbank Builder

为不同候选人与目标岗位构建事实受控、口语化、可直接练习的面试材料，并将结果保存到按公司划分的题库目录。

## 核心原则

1. 零虚构与事实受控：严格区分独立主导、核心参与、协助协同和仅了解，不能把 JD 要求或外部面经写成候选人事实。
2. 前置资料门禁：开始生成前必须确认候选人背景、目标岗位 JD、知识库或事实材料、公司名称、岗位名称和输出根目录。
3. 小红书检索默认关闭：小红书不是默认步骤。只有用户明确提出搜索小红书、找小红书面经或参考小红书高频题等要求时，才开启 agent-reach 检索；用户未明确要求时，不检查登录态、不调用 xhs，也不因未检索小红书而阻塞题库生成。
4. 小红书外部内容不等于候选人事实：开启后，内容只用于识别岗位面试风格、常见追问和高频主题，不能证明候选人做过任何项目或掌握任何技能。
5. 回答采用统一表达风格：使用稳健工科口语风：教育背景定锚 -> 岗位相关经历 -> 具体动作与验证 -> 能力总结 -> 岗位落点与谦逊意愿。
6. 回答采用三段论：结论先行 -> 真实案例或量化证据 -> 落到目标岗位。语言应自然、克制、适合现场口述。

## 工作流

### 第一步：采集并确认前置资料

在生成任何题库或自我介绍前，确认以下信息：

1. 候选人真实背景：学历、专业、实习或工作经历、项目、职责边界、技术栈和熟练程度。
2. 目标岗位 JD：公司、岗位名称、职责、任职要求、技术关键词和面试轮次（如已知）。
3. 知识库或事实材料：Obsidian、语雀、Notion、飞书、本地文件、工作日志或项目链接；若没有，必须明确回复无或以当前提供材料为准。
4. 输出信息：公司名称、岗位名称、题库根目录。根目录未提供时，可使用当前环境中明确的默认目录；多人复用场景应先询问，不要擅自使用个人路径。

信息缺失时，一次性列出缺口并等待补充，不要依据岗位名称或常见简历模板猜测候选人经历。

### 第二步：确定输出路径并读取已有题库风格

默认输出结构：

    <题库根目录>/<公司名称>/<公司名称>-<岗位名称>-面试题库.md

执行规则：

- 公司名称缺失或不确定时，不创建文件；
- 先检查公司文件夹是否存在；不存在时创建；
- 同一公司和岗位已有题库时，先读取并判断是更新原题库还是生成新版本；
- 默认保留已有文件，不删除历史版本；
- 未经用户明确要求，不覆盖旧题库；
- 写入后验证最终文件确实位于公司文件夹中。

在生成自我介绍前，扫描题库根目录下现有的 *.md 和 *.txt，提取其中的自我介绍、个人介绍、一分钟版本、30 秒版本、风格定位和口语化说明。只抽象表达规律，不复制其他候选人的姓名、公司、学校、项目或具体事实。

统一采用稳健工科口语风：

1. 教育背景定锚：学校、学历、专业或研究方向；
2. 岗位相关经历：只选择 1~2 段最匹配经历；
3. 具体动作与验证：说明做了什么、如何验证或形成什么结果；
4. 能力总结：提炼 1~2 个岗位相关能力，不堆砌技术名词；
5. 岗位落点：说明希望如何迁移到目标岗位，并表达愿意从具体模块或工程环节做起。

风格约束：

- 一分钟版本通常控制在约 220~280 个汉字，30 秒版本约 120~160 个汉字；
- 技术名词必须附着在真实动作后面出现；
- 优先使用参与过、做过、负责其中的、形成了一定理解、希望从具体模块做起等克制表达；
- 避免精通、全面负责、独立完成整套系统、完全胜任等无证据或过度承诺表述；
- 课程项目不能包装成真实生产交付，团队成果不能包装成个人独立成果；
- 不复制题库中的具体个人事实，风格可迁移，事实必须重新从当前候选人材料提取。

### 第三步：建立事实边界和 JD 映射

- 提取已证实事实、待确认事实、不可声称事实三类清单；
- 将 JD 映射为高匹配区、可迁移区和能力空白区；
- 对每个核心项目记录候选人角色：主导、参与、协助或了解；
- 将所有不确定数字、技术深度和项目归属标记为待确认，不自动补齐。

### 第四步：可选开启小红书面经检索

默认跳过本步骤。只有用户明确要求搜索小红书或参考小红书面经时，才执行以下流程。

#### 4.1 先确认用户意图

如果用户只说准备面试题库或生成题库，视为未授权小红书检索，不调用 xhs。

如果用户明确要求小红书检索，先告知将读取公开面经样本，并使用 agent-reach 的 xhs CLI。随后执行：

    xhs status
    xhs whoami

出现未认证、登录过期、Session expired、No a1 cookie found、Cookie 缺失、渠道未配置或搜索无法可靠执行时：

- 立即暂停小红书模块和后续题库生成；
- 告知用户先完成浏览器或扫码登录；
- 不索取、不保存、不展示 Cookie、Token 或本地存储内容；
- 用户回复已登录后重新检查 xhs status 和 xhs whoami；
- 认证通过后才继续搜索。

#### 4.2 检索与整理

根据公司、岗位、行业和面试轮次生成 2~4 个查询，至少覆盖公司名 + 岗位名 + 面试、岗位名 + 面试题、公司名 + 面经、行业或技术方向 + 岗位名 + 高频题。

使用 xhs search 获取结果；需要核实时，只使用搜索结果中的 URL 或带上下文的 ID 调用 xhs read 或 xhs comments，不要使用裸 note_id。保持低频请求，遇到验证码或风控时停止批量操作。

题库中增加小红书检索摘要，标明查询词、代表性来源、重复高频主题、单样本线索和搜索限制。小红书内容只影响问什么和追问方向，不改变候选人事实。

### 第五步：生成一分钟和 30 秒自我介绍

- 依据当前候选人事实和统一风格生成；
- 一分钟版本约 220~280 个汉字；
- 30 秒版本约 120~160 个汉字；
- 只选择与目标岗位最匹配且已证实的 1~2 个经历；
- 语言沉稳、谦逊、自然，不加入没有证据的成果或职责。

### 第六步：生成 20~30 道面试题库

题库建议覆盖：

1. 项目深挖与技术实战题（8~12 题）；
2. 岗位职责与工作方法题（4~6 题）；
3. 跨部门协同、抗压和异常处理题（4~6 题）；
4. 求职动机与职业规划题（4~6 题）。

每题包含：题目、考察重点、参考回答（结论 -> 真实案例或量化 -> 落到岗位）、可能追问、避坑提示。

### 第七步：反问清单、速记卡与写入验证

输出 3~5 个高价值反问、3 个核心经历标签、2 条表达红线和 1 句岗位匹配总结。写入文件后检查：

- 路径是否为题库根目录/公司名称/公司-岗位-面试题库.md；
- 是否保留了事实边界和来源说明；
- 是否明确小红书检索为未开启或包含检索摘要；
- 是否能在正常语速下完成自我介绍。

## 质量核验清单

- [ ] 候选人背景、JD、知识库、公司名称、岗位名称和输出根目录是否已确认？
- [ ] 是否扫描了现有题库并抽象统一的自我介绍风格？
- [ ] 未明确要求小红书时，是否没有调用 xhs 或检查登录态？
- [ ] 明确要求小红书时，是否先执行认证检查？
- [ ] 登录态有问题时，是否暂停并等待用户，而不是继续生成？
- [ ] 小红书内容是否与候选人事实分开？
- [ ] 所有项目、数字、技术选型和职责是否有来源？
- [ ] 每题是否包含考察重点、三段式回答、追问和避坑提示？
- [ ] 自我介绍是否符合稳健工科口语风？
- [ ] 文件是否写入正确的公司目录并完成读回验证？
